# 04 · 记忆系统全解析

> 📖 **函数级明细**：本文末 附录 M · 记忆系统逐函数手册（middleware/STM/LTM/抽取器全函数，含每处历史踩坑标注）。


> **流程图（必看）**：[00-全流程图集.md](00-全流程图集.md) 的 §11 before/after、§12 压缩抽取、§13 P0–P3 注入。

## 0.1 学习导航

### 这一章的核心目标

你不是要把“记忆系统”当成一个抽象名词记住，而是要回答清楚：

1. 为什么这里不是简单地把历史消息全拼进 Prompt。
2. 为什么要有 P0、P1、P2、P3 四层。
3. 为什么记忆读写要挂在 `before_agent` / `after_agent`。

### 读这一章前最好先知道什么

1. 记忆系统并不只等于 Redis。
2. 画像、摘要、长期记忆虽然都和“记住用户”有关，但作用完全不同。
3. AI 应用里的记忆重点不只是“能记住”，还包括“记错了怎么办、依赖挂了怎么办”。

### 这一章学完后你应该会什么

1. 能解释 P0-P3 分层及其优先级。
2. 能讲清 `before_agent` 读什么、`after_agent` 写什么。
3. 能解释 STM 为什么要压缩，LTM 为什么要检索和去重。
4. 能讲清删除会话时为什么要连带清理 STM / LTM。

### 推荐阅读方法

1. 先看 §0 图和 §1 分层，建立整体视图。
2. 再看 §3 MemoryMiddleware 编排，把读写顺序吃透。
3. 再看 §4 STM、§5 LTM，区分短期和长期能力。
4. 最后看“面试深挖”和附录 B，转成面试问答。

### 常见误区

1. 把画像和长期记忆混成一个概念。
2. 只会说“Redis 存最近窗口消息”，但讲不清为什么还需要摘要和压缩。
3. 认为记忆失败就等于主对话一定失败，没有意识到这里做了降级。

## 0. 记忆总览（图）

```mermaid
flowchart LR
    REQ[问答请求] --> BEFORE["before_agent 读<br/>（读扩散：三路并发）"]
    BEFORE --> P0[(P0 Redis 最近消息)]
    BEFORE --> P1[(P1 MySQL 画像)]
    BEFORE --> P2[(P2 Redis 摘要)]
    BEFORE --> P3[(P3 Milvus LTM)]
    P0 & P1 & P2 & P3 --> CTX[build_memory_context]
    CTX --> AGENT[LLM / 检索]
    AGENT --> AFTER[after_response]
    AFTER -->|"publish turn_completed<br/>turn_id/event_id"| EV[["Redis Streams<br/>agent:events"]]
    EV --> INBOX[("MySQL processed_events<br/>Inbox claim")]
    INBOX --> CONSUMER["消费者<br/>（写扩散起点，异步）"]
    INBOX -->|"completed 重放"| ACK["仅 XACK"]
    CONSUMER --> H[(MySQL messages 历史)]
    CONSUMER --> W1[写 STM 消息窗口]
    W1 --> W2{达到压缩阈值?}
    W2 -->|是| W3[LLM 摘要 → 抽取 LTM → 更新画像]
    W2 -->|否| DONE[本轮写扩散结束]
    W3 --> DONE
```

> 读半边是**同步并发**（用户在等答案，延迟敏感）；写半边经
> `turn_completed` 事件**异步展开**（答案已发出，可靠性优先于时延）。Streams
> 仍是至少一次投递；消费者先由 MySQL Inbox 按 `(event_type, turn_id)` 认领，
> 已完成事件只 ACK，防止崩溃重放重复扩散。
> 这一对选择正是「读扩散 / 写扩散」的教科书式分工，详见 §1.5。

```mermaid
flowchart TB
    subgraph PriorityPyramid["P0-P3 记忆注入优先级金字塔 (权威度: P0 > P1 > P2 > P3)"]
        direction TB
        P0["P0: 最近消息 (Redis STM ZSET 窗口)\n最新 16 条对话上下文，最高权威度与格式覆盖"]
        P1["P1: 用户画像 (MySQL user_profiles + Redis Cache)\n用户持久偏好、品牌/预算/品类/版本化事实 Facts"]
        P2["P2: 会话摘要 (Redis STM Session Summary)\n超过触发轮次后 LLM 生成的历史对话压缩摘要"]
        P3["P3: 长期记忆 (Milvus LTM Collection)\n跨会话沉淀的 issue_history 与 solution_note 向量检索"]
        P0 === P1 === P2 === P3
    end
```

## 1. 目标与分层

| 层级 | 存储 | 时效 | 职责 |
|---|---|---|---|
| **P0 最近消息** | Redis STM ZSET 窗口 | TTL 默认 24h | 当前会话权威上下文 |
| **P1 用户画像** | MySQL + Redis cache | durable | 品牌/预算/品类/标签/facts |
| **P2 会话摘要** | Redis STM summary | 随会话 | 压缩后的旧轮次 |
| **P3 长期记忆** | Milvus LTM | durable + 衰减/命中 | issue_history / solution_note |

冲突时优先级：**P0 > P1 > P2 > P3**（注入文案写死在 `memory_context.py`）。

### 1.1 完整模块地图（树状图 + 逐文件）

#### 树状图（记忆相关）

```text
app/knowledge/
├── README.md
├── domain/
│   ├── schemas.py              # MessageRecord / LTM / AgentMemoryState...
│   └── prompt_builder.py       # 压缩与注入 Prompt
└── infrastructure/
    ├── stm/
    │   ├── redis_short_term_memory.py
    │   └── stm_compressor.py
    ├── ltm/
    │   ├── simple_long_term_memory.py
    │   └── ltm_collection.py
    └── orchestration/
        ├── memory_middleware.py    # before/after 中枢
        ├── memory_extractor.py
        └── profile_adapter.py

app/chat/infrastructure/graph/
├── memory_context.py           # load/enrich/P0–P3 拼装
└── lifecycle_nodes.py          # after_response

app/user/                       # P1 画像（详见 07）
├── domain/schemas.py
├── domain/profile_payload_support.py
├── application/user_profile_service.py
└── infrastructure/repository/user_profile_repository.py

app/shared/core/app_config.py   # STM/LTM 默认配置
app/shared/retrieval/milvus_hybrid_core.py
app/platform/container.py       # 装配 middleware
```

#### knowledge 域（记忆主体）

| 文件 | 用处 |
|---|---|
| `app/knowledge/README.md` | 域边界：记忆+索引，非 HTTP |
| `domain/schemas.py` | `MessageRecord`/`SessionMeta`/`SessionSummary`/`LongTermMemory`/`AgentMemoryState`/`MemorySearchResult`/`MemoryExtractorResult` |
| `domain/prompt_builder.py` | 压缩 Prompt、LTM/摘要注入 Prompt 拼装 |
| `infrastructure/stm/redis_short_term_memory.py` | STM 读写、压缩触发、`clear_session` |
| `infrastructure/stm/stm_compressor.py` | `compress_message`/`decompress_message`（MsgPack+Zstd） |
| `infrastructure/ltm/simple_long_term_memory.py` | `save_memory`/`hybrid_search`/`deduplicate`/`soft_delete_session_memories`/hit 更新 |
| `infrastructure/ltm/ltm_collection.py` | collection 名、字段列表、`MEMORY_OUTPUT_FIELDS`、filter 构造 |
| `infrastructure/orchestration/memory_middleware.py` | `before_agent` / `after_agent` 编排中枢 |
| `infrastructure/orchestration/memory_extractor.py` | 压缩后 LLM 抽取语义记忆+画像候选 |
| `infrastructure/orchestration/profile_adapter.py` | `load_user_profile`/`save_user_profile` → user 域 |

#### chat 域（主图挂载点）

| 文件 | 用处 |
|---|---|
| `chat/infrastructure/graph/memory_context.py` | `load_memory_state`/`enrich_question`/`build_memory_context`/P0–P3 标题 |
| `chat/infrastructure/graph/lifecycle_nodes.py` | `after_response` 生成 `turn_id` 并发布事件；消费者再调 middleware.after_agent |
| `chat/application/conversation_service.py` | 删会话时 `_clear_conversation_memories` |

#### user 域（画像持久化，P1）

| 文件 | 用处 |
|---|---|
| `user/domain/schemas.py` | `UserProfileData`/`UserProfileFact`/`UserProfilePayload` |
| `user/domain/profile_payload_support.py` | 规范化 tags/facts/可选文本 |
| `user/application/user_profile_service.py` | get/upsert + Redis 缓存 |
| `user/infrastructure/repository/user_profile_repository.py` | MySQL 画像与 facts 版本 |

#### 配置与共享

| 文件 | 用处 |
|---|---|
| `shared/core/app_config.py` | `STMConfig`/`LTMConfig`/`MemoryConfig` 默认值 |
| `shared/retrieval/milvus_hybrid_core.py` | LTM hybrid 底层 dense/sparse |
| `platform/container.py` | `_create_memory_middleware` 装配 |

---

## 2. 核心类型

文件：`app/knowledge/domain/schemas.py`、`app/user/domain/schemas.py`

| 类型 | 所属 | 字段要点 |
|---|---|---|
| `MessageRecord` | knowledge | message_id, role, content, created_at, turn_index |
| `SessionMeta` | knowledge | total_turns, last_updated_at, last_compressed_turn |
| `SessionSummary` | knowledge | content, compressed_at, compressed_round |
| `LongTermMemory` | knowledge | memory_id, tenant/user, **session_id**, type, content, hit_count, is_deleted… |
| `MemorySearchResult` | knowledge | memory + score |
| `MemoryExtractorResult` | knowledge | 抽取候选项 |
| `AgentMemoryState` | knowledge | 一次请求快照：summary/recent/ltm/profile |
| `UserProfileData` | **user** | preferred_brand/budget_range/preferred_category/tags/facts |
| `UserProfilePayload` | **user** | + user_id |

**边界：** 画像类型在 user 域；记忆快照在 knowledge 域引用它。

---

## 2.5 写扩散与读扩散 —— 记忆系统的核心设计范式 ⭐

> 这是理解整个记忆系统"为什么长这样"的钥匙。搞懂这一节，
> §3 的编排代码就只是这个范式的直译。

### 2.5.1 概念：一分钟版

这对概念来自 Feed 流系统设计（微博时间线是经典案例）：

| | 写扩散（fan-out on write） | 读扩散（fan-out on read） |
|---|---|---|
| 核心思想 | **写入时**就把数据加工成各消费方需要的形态（物化视图） | **读取时**才现场从多个来源拉取并组装 |
| 写成本 | 高（一次写 → N 份视图，写放大） | 低（只写一份原始数据） |
| 读成本 | 低（读现成的） | 高（读时 N 路查询 + 组装） |
| 数据新鲜度 | 视图可能滞后（异步物化） | 永远是最新的 |
| 适用 | 读远多于写、读延迟敏感 | 来源异构无法预 join、写远多于读 |

微博的取舍：给大 V 发帖做写扩散（推给千万粉丝收件箱）会写爆，
所以大 V 走读扩散、普通人走写扩散——**混合**才是常态。本系统同样是混合。

### 2.5.2 本系统的写扩散：一轮对话 → 五个物化视图

用户的一轮对话（user + assistant 两条消息）在 `turn_completed` 事件的
消费者里被**扩散写**到五个针对不同读场景优化的视图：

```mermaid
flowchart TB
    TURN["一轮对话完成<br/>(user_message, assistant_message)"]
    TURN -->|"after_response 发布稳定 turn_id"| EV[["turn_completed 事件<br/>Redis Streams / 崩溃可重放"]]
    EV --> I[("MySQL Inbox<br/>claim(type, turn_id)")]
    I -->|PROCESS| C["消费者 handle_turn_completed"]
    I -->|completed| ACK["仅 XACK"]

    C --> V1["视图① MySQL messages<br/>append-only 完整历史<br/><i>读场景：人看——切回旧会话、审计</i>"]
    C --> V2["视图② Redis STM 消息窗口<br/>ZSET 滑动窗口 + 压缩编码<br/><i>读场景：模型看——P0 最近上下文</i>"]
    V2 --> COND{"达到压缩阈值?<br/>(6 轮 或 20 条)"}
    COND -->|是| V3["视图③ Redis 会话摘要<br/>LLM 压缩旧消息<br/><i>读场景：P2 长对话的廉价上下文</i>"]
    V3 --> V4["视图④ Milvus LTM<br/>LLM 抽取语义记忆 + embedding<br/><i>读场景：P3 跨会话语义检索</i>"]
    V3 --> V5["视图⑤ MySQL 画像 + Redis 缓存<br/>LLM 提炼偏好/事实<br/><i>读场景：P1 稳定用户特征</i>"]
    COND -->|否| END[本轮到此为止]
```

三个刻意的设计决策：

1. **写扩散必须异步**。视图③④⑤各需要一次 LLM 调用（摘要/抽取/提炼），
   动辄数秒。答案已经通过 SSE 发出，用户不该为"系统内部记账"等待——
   所以 `after_response` 只发布事件就返回，扩散在消费者里展开。
2. **异步就必须可靠且可重放**。fire-and-forget 协程随进程共存亡；事件在
   Redis Streams 的 PEL 里，进程崩溃后被 `XAUTOCLAIM` 认领重放，失败有重试
   上限与死信流。消费前 Inbox 用 `(event_type, turn_id)` 租约串行认领，完成后才
   `XACK`；同一 `turn_id` 又会被 MySQL history 唯一键与 STM 回合标记防重。
   **写扩散丢一环，对应优先级的记忆就永久缺失**（摘要丢了 P2 就空、抽取丢了
   P3 就空），可靠性不是锦上添花。
3. **昂贵的视图按需物化**。①②每轮都写（便宜）；③④⑤只在压缩阈值
   触发时才做（LLM 成本高、且积累几轮后摘要/抽取的语义更完整）。
   这是写扩散的成本控制阀门——不是所有视图都值得每次写。

### 2.5.3 本系统的读扩散：一次提问 → 三路并发拉取

`before_agent` 在每轮问答开始时，从三套**异构存储**现场拉取并组装上下文：

```mermaid
flowchart LR
    Q[用户提问] --> BA[before_agent]
    BA -.->|asyncio.gather 并发| R1 & R2 & R3
    subgraph 读扩散["三路读扩散（互不依赖）"]
        R1["_read_short_term<br/>Redis: 摘要 + 最近消息<br/>(视图②③)"]
        R2["_read_user_profile<br/>MySQL+Redis缓存: 画像<br/>(视图⑤)"]
        R3["_read_long_term<br/>Milvus: embedding + 混合检索<br/>(视图④，最慢的一路)"]
    end
    R1 & R2 & R3 --> ASM["build_memory_context<br/>按 P0>P1>P2>P3 组装注入"]
    ASM --> LLM[进入主图推理]
```

为什么这里选读扩散而不是"再物化一层大宽表"？

- **无法预 join**：P3 长期记忆的召回依赖**当前问题**的语义
  （embedding 相似度），提问之前不存在"可以预先算好的结果"。
  这是读扩散的硬理由——查询参数是读时才知道的。
- **来源异构**：Redis ZSET、MySQL 行、Milvus 向量，三种数据模型
  硬拼成一份物化视图，维护成本远超三路并发读。
- **读扩散的代价用并发抵掉**：三路互不依赖，`asyncio.gather` 让总延迟
  = max(三路) 而不是 sum(三路)。这就是 v3.33 那轮性能优化的本质：
  **读扩散系统的第一优化永远是"把串行读改并发"**。

### 2.5.4 合起来看：一条数据的完整生命周期

```mermaid
sequenceDiagram
    autonumber
    participant U as 用户
    participant API as SSE 接口
    participant G as LangGraph
    participant EV as Redis Streams
    participant I as MySQL Inbox
    participant C as 事件消费者
    participant S as 五个视图存储

    Note over U,S: ── 第 N 轮：读扩散（同步，延迟敏感）──
    U->>API: 提问
    API->>G: astream
    G->>S: before_agent 三路并发读（现成视图）
    S-->>G: P0-P3 上下文
    G-->>U: SSE 流式答案（用户等待到此为止）

    Note over U,S: ── 第 N 轮：写扩散（异步，可靠性敏感）──
    G->>EV: publish turn_completed {turn_id,event_id}
    EV->>C: 消费（崩溃则 XAUTOCLAIM 重放）
    C->>I: claim(type, turn_id, payload_hash)
    alt 已完成事件
        I-->>C: SKIP_COMPLETED
        C->>EV: XACK（无业务副作用）
    else 可处理事件
        I-->>C: PROCESS
        C->>S: 历史 → STM → (阈值) 摘要 → LTM → 画像
        C->>I: mark_completed
        C->>EV: XACK
    end
    Note over C,S: 第 N+1 轮的读扩散读到的就是这里物化的视图
```

**一句话总结**：写扩散把"贵"移到用户看不见的异步链路里换取读时便宜；
读扩散处理"读时才知道查询条件"的场景并用并发压延迟。
两者不是二选一，而是按每类数据的读写特征分别选择。

### 2.5.5 什么情况下这套取舍要重新评估

教学式文档不该只讲"现在为什么对"，还要讲"什么时候会变错"：

| 变化 | 受冲击的选择 | 演进方向 |
|---|---|---|
| 单用户 QPS 变高（如机器人刷问） | 写扩散的 LLM 成本被放大 | 压缩阈值调高 / 抽取采样化 |
| LTM 数据量暴涨、检索变慢 | 读扩散最慢一路拖高 max() | 给 P3 加超时预算，超时放弃该路（部分读扩散） |
| 需要"用户全景页"（一次看全部记忆） | 读扩散每次现拼太贵 | 为该页面**增加**一个写扩散视图（新消费者订阅同一事件） |
| 多租户隔离要求 | 两者都要加 tenant 过滤 | 事件与视图 key 已带 tenant_id，扩展点已留好 |

> 注意最后一行的模式：**新增读场景 = 给同一个事件流加一个新消费者**，
> 而不是改写现有链路。这是把写扩散建在事件管线上的最大红利。

### 面试深挖：写扩散/读扩散

**Q: 为什么消息要同时写 MySQL 和 Redis STM，不是冗余吗？**
A: 是刻意的双视图，读场景完全不同：MySQL messages 是给**人**看的
append-only 完整历史（永不参与推理）；STM 是给**模型**看的滑动窗口
（16 条上限 + 24h TTL + 压缩编码）。用一份数据同时服务两种读，
要么历史被窗口截断，要么模型上下文无限膨胀。

**Q: 写扩散链路里某一环失败会怎样？**
A: 分两层。环与环之间：历史落库失败不阻断记忆写入（互相独立降级）；
事件层面：整个 handler 抛出才触发 stream 重试，最多 3 次后进死信流
（可人工检查）。设计原则是"单环失败只缺对应视图，绝不放大成整轮丢失"。

**Q: 读扩散三路里有一路挂了怎么办？**
A: 每路独立 try/except 降级为空值（该优先级的记忆缺席），其余照常注入。
记忆是增强项——三路全挂，对话也必须能继续（裸 LLM 模式）。

---

## 3. MemoryMiddleware 编排

文件：`app/knowledge/infrastructure/orchestration/memory_middleware.py`  
装配：`app/platform/container.py::_create_memory_middleware`

```mermaid
sequenceDiagram
    autonumber
    participant Agent as LangGraph Agent 主图
    participant MM as MemoryMiddleware
    participant STM as Redis STM
    participant Profile as ProfileAdapter (MySQL+Redis)
    participant LTM as SimpleLongTermMemory (Milvus)
    participant Extractor as MemoryExtractor (LLM)

    rect rgb(240, 248, 255)
        note over Agent, LTM: Phase 1: before_agent (请求前上下文读取)
        Agent->>MM: before_agent(tenant, user, session, query)
        MM->>STM: get_summary + get_recent_messages
        STM-->>MM: 返回 P0 最近消息 & P2 会话摘要
        MM->>Profile: load_user_profile(user_id)
        Profile-->>MM: 返回 P1 用户画像数据与 Facts
        MM->>LTM: hybrid_search(user_input, top_k)
        LTM-->>MM: 返回 P3 长期记忆 (issue_history/solution_note)
        MM-->>Agent: 返回 AgentMemoryState (组装完成)
    end

    rect rgb(255, 245, 238)
        note over Agent, Extractor: Phase 2: after_agent (回复后写回与异步归档)
        Agent->>MM: after_agent(tenant, user, session, user_msg, ai_msg)
        MM->>STM: append_message(user_msg) & append_message(ai_msg)
        MM->>STM: total_turns += 1 & refresh_ttl()
        
        alt 触发压缩条件 (should_compress == True)
            MM->>STM: compress_session_memory(LLM summary)
            STM-->>MM: 返回 new_summary
            MM->>Extractor: extract(user_msg, ai_msg, new_summary)
            Extractor-->>MM: 返回 MemoryExtractorResult (LTM 记忆 + 画像 Candidate)
            opt 抽取到 LTM 记忆
                MM->>LTM: save_memory(issue_history/solution_note)
            end
            opt 抽取到新 Facts/偏好
                MM->>Profile: save_user_profile(updated_facts)
            end
        end
    end
```

依赖注入：

- `redis_stm: RedisShortTermMemory`  
- `milvus_ltm: SimpleLongTermMemory`  
- `memory_extractor: MemoryExtractor`  
- `profile_reader/writer` 默认 `profile_adapter.load/save_user_profile`  

### 3.1 `before_agent(tenant, user, session, user_input)`

三路读取落在三套互不相干的存储上、彼此无数据依赖，因此 **并发发起**
（`asyncio.gather`）——耗时从「三者之和」降为「三者最大值」。
这是每轮对话的必经路径，其中 LTM 还要跑 embedding + 向量检索，通常最慢。

```text
并发执行以下三路，任一路失败只降级该路：

  A. _read_short_term:  get_summary ‖ get_recent_messages
                        失败 → (None, [])
  B. _read_user_profile: user_id 可解析为 >0 才读 profile_reader(uid, redis)
                        匿名 → None（不覆盖默认画像）；失败 → {}
  C. _read_long_term:   ltm_enabled 才 milvus_ltm.hybrid_search(...)
                        未启用或失败 → []

汇总为 AgentMemoryState 返回
```

> 并发的副作用：三路降级告警的**到达顺序不确定**，断言时不要依赖顺序。

### 3.2 `after_agent(...)`

> **执行时机（v3.35.0 起）**：`after_response` 图节点**发布 `turn_completed`
> 事件**（Redis Streams），由消费者执行"MySQL 历史落库 + 本函数"；
> 进程崩溃后消息被 XAUTOCLAIM 认领自动重放，失败有重试上限与死信。
> `turn_completed` 带稳定的 `turn_id/event_id`：Inbox 已完成则直接 ACK；即使
> 业务已经成功、但 `mark_completed` 或 XACK 尚未来得及完成，MySQL 历史和 STM
> 仍会按该 ID 防重。
> 事件基础设施不可用时回退为进程内 fire-and-forget（v3.34.0 行为，
> `flush_pending_memory_writes()` 仍可等待在途写入）。
> 无论哪条路径，记忆写入都不在 SSE 关键路径上。

```text
1. 若有 `turn_id`，`append_turn_once` 先获取会话短锁，检查 `turns` 标记；
   首次处理在一个 Redis transaction 内写 user/assistant 两条、meta 和标记；
   重放直接返回已有 meta（不重复增加轮次）
2. 刷新 messages/summary/meta TTL
3. 复用上述 meta；若 should_compress:
     compress_session_memory(summary_compressor=LLM)
     若压缩成功且 ltm_enabled:
     extractor.extract(user, assistant, new_summary)
     → 语义记忆 dedup + save_memory
     → profile 非空则 profile_writer
4. 可选：update_memory_hit_infos(全部命中)  ← 一次批量 upsert，不逐条往返
```

当压缩后产生 LTM 语义记忆且存在 `turn_id` 时，`MemoryMiddleware` 以
`UUID5(turn_id, memory_type, content)` 派生确定性 `memory_id`，并让 LTM 走
upsert；这覆盖了“STM 已写成功、Inbox completed 还未落库”后重放的最深一层落点。

所有 Redis/Milvus 异常以**降级 + 单次告警**为主，避免刷屏和拖垮主对话。

**降级日志分级（v3.33.0 起）**——统一走 `app.shared.core.degradation.log_degradation`：

| 异常类别 | 判定 | 日志级别 | 是否带堆栈 |
|---|---|---|---|
| Redis 错误 / 超时 / 连接失败 / OSError | 可预期的外部依赖故障 | `warning` | 否 |
| 其余全部（TypeError、AttributeError…） | **疑似代码缺陷** | `error`（`logger.exception`） | **是** |

> WHY 要分级：此前两类共用一句无异常信息的 warning，代码缺陷和网络抖动在日志里
> 长得一模一样。项目真实踩过——LTM 配置是 frozen dataclass 却被按 dict 下标取值，
> 每次调用抛 `TypeError` 被吞掉，表现为「长期记忆静默失效」：不报错，
> 只是永远检索不到、也永远写不进去。

#### 3.2.1 源码级入口链路：回答完成后如何进入 `after_agent`

> **源码锚点**：
> `app/chat/infrastructure/graph/lifecycle_nodes.py::after_response` 生成 `turn_id` 并发布回合事件；
> `app/platform/events.py::handle_turn_completed` 消费事件并调用本函数；
> `app/knowledge/infrastructure/orchestration/memory_middleware.py::MemoryMiddleware.after_agent` 是真正的写扩散执行体。

~~~mermaid
flowchart TD
    A[LangGraph after_response 节点] --> B{是否同时拿到最后一条用户消息和助手消息}
    B -->|否| Z[跳过记忆写入]
    B -->|是| C[生成 turn_id=turn_xxx<br/>event_created_at=当前秒级时间]
    C --> D[发布 Redis Stream<br/>agent:events / turn_completed]
    D -->|发布成功| E[RedisStreamQueue 消费组 core]
    D -->|发布失败| F[进程内 create_task<br/>_write_turn_memory]
    E --> G[EventInbox claim<br/>按 event_type + event_id 幂等认领]
    G -->|completed| H[XACK 后跳过业务副作用]
    G -->|busy| I[保留在 PEL 等待后续重试]
    G -->|payload_conflict| J[标记 failed 并进入错误路径]
    G -->|process| K[handle_turn_completed]
    K --> L[_persist_turn_history<br/>MySQL messages 追加 user + assistant]
    K --> M[container.memory_middleware.after_agent]
    F --> M
    M --> N[STM / 压缩 / LTM / 画像 / hit 统计]
~~~

这条链路有四层幂等，分别覆盖 Stream 消费、可见历史、STM 窗口和 LTM 语义记忆：

| 层级 | 幂等字段 | 约束/写法 | 解决的问题 |
|---|---|---|---|
| 事件消费 | `processed_events(event_type, event_id)` | MySQL 唯一键 `uk_processed_event` + lease | 同一 Stream 事件只允许一个消费者执行业务处理 |
| 历史消息 | `messages(conversation_id, turn_event_id, sender)` | MySQL 唯一键 `uk_messages_turn_event_sender` | XACK 前崩溃重放时，不重复追加可见历史 |
| STM 回合 | Redis `turns` Hash 的 `turn_id` | `append_turn_once` 在 transaction 内写消息、meta、turn 标记 | 重放时不重复增加 `total_turns`、不重复写 user/assistant |
| LTM 记忆 | `memory_id = uuid5(turn_id:memory_type:content)` | 有 `turn_id` 时 `save_memory` 走 Milvus upsert | STM 已写成功但 completed/ACK 前崩溃时，抽取出的同一条 LTM 不重复 |

#### 3.2.2 函数签名与三段职责

~~~python
async def after_agent(
    self,
    tenant_id: str,
    user_id: str,
    session_id: str,
    user_message: str,
    assistant_message: str,
    long_term_memories: list[MemorySearchResult] | None = None,
    *,
    turn_id: str = "",
    event_created_at: int | None = None,
) -> None
~~~

| 阶段 | 源码动作 | 主要落点 | 失败影响 |
|---|---|---|---|
| 1. STM 落盘 | 写入本轮 user/assistant、更新 `SessionMeta`、刷新 TTL | Redis STM | 只降级 `memory.write_short_term`，后续会尝试回源读取 meta |
| 2. 压缩 + 抽取 | 判断 `should_compress`，压缩旧消息，抽取 LTM 和画像 | Redis summary、Milvus LTM、MySQL 画像 | 只降级 `memory.compress_and_extract`，不影响用户已收到的回答 |
| 3. 命中统计 | 对传入的 `long_term_memories` 批量刷新 hit 信息 | Milvus partial upsert | 只降级 `memory.refresh_ltm_hits` |

注意：当前主图事件路径只把 `tenant_id/user_id/session_id/user_message/assistant_message/turn_id/event_created_at` 传给 `after_agent`，**没有透传 `before_agent` 读到的 `long_term_memories`**；因此第 3 阶段在主路径通常不执行，只有测试、直调或未来扩展把命中列表传入时才会刷新命中统计。

#### 3.2.3 内部流程图：STM → 压缩 → LTM/画像 → hit 统计

~~~mermaid
flowchart TD
    A[after_agent 入参] --> B[now_ts = event_created_at or time.time]

    subgraph S1[阶段 1：STM 落盘]
        B --> C{turn_id 存在且 STM 支持 append_turn_once}
        C -->|是| D[append_turn_once<br/>抢 turn_lock]
        D --> E{turns Hash 已有 turn_id}
        E -->|是| F[返回 inserted=False + 既有 meta<br/>不重复写消息]
        E -->|否| G[Redis transaction<br/>ZADD 两条消息<br/>SET meta<br/>HSET turns]
        C -->|否| H[兼容旧路径<br/>get_meta -> total_turns+1<br/>append_messages -> save_meta]
        F --> I[refresh_ttl]
        G --> I
        H --> I
    end

    subgraph S2[阶段 2：压缩 + 抽取]
        I --> J{meta 是否可用}
        J -->|否| K[get_meta 回源]
        J -->|是| L[get_message_count]
        K --> L
        L --> M{should_compress}
        M -->|否| X[跳过压缩和抽取]
        M -->|是| N[compress_session_memory]
        N --> O[并发读 meta / msg_count / summary / recent messages]
        O --> P[build_compression_context<br/>切分待压缩旧消息与保留消息]
        P --> Q[run_compression_pipeline<br/>抢 lock]
        Q --> R[summary_compressor<br/>build_compression_prompt -> extractor.llm_client.ainvoke]
        R --> S[save_summary 若 LLM 返回可解析 JSON]
        S --> T[rewrite_recent_messages<br/>只保留 keep_recent_rounds * 2 条]
        T --> U[save_meta<br/>last_compressed_turn = total_turns]
        U --> V{compressed 且 ltm_enabled}
        V -->|否| X
        V -->|是| W[get_summary -> memory_extractor.extract]
        W --> Y[语义记忆逐条 deduplicate_memory]
        Y --> AA{是否需要新增}
        AA -->|否| AB[跳过该候选]
        AA -->|是| AC[save_memory<br/>session_id + 可选确定性 memory_id]
        W --> AD{profile 非空且 user_id 为正整数}
        AD -->|是| AE[profile_writer -> user_profiles / user_facts]
        AD -->|否| AF[跳过画像写入]
    end

    subgraph S3[阶段 3：命中统计]
        X --> AH{long_term_memories 是否非空}
        AB --> AH
        AC --> AH
        AE --> AH
        AF --> AH
        AH -->|是| AI[update_memory_hit_infos<br/>批量 partial upsert]
        AH -->|否| AJ[结束]
        AI --> AJ
    end
~~~

#### 3.2.4 结构表：事件、MySQL、Redis、Milvus、画像

##### A. `turn_completed` 事件 payload

| 字段 | 来源 | 类型/示例 | 下游用途 |
|---|---|---|---|
| `event_id` | `after_response` | 与 `turn_id` 相同 | Inbox 的幂等主键；缺失时会从 `turn_id/task_id/stream entry` 派生 |
| `turn_id` | `after_response` | `turn_` + uuid hex | MySQL `turn_event_id`、STM `turns` Hash、LTM 确定性 `memory_id` 的种子 |
| `event_created_at` | `after_response` | 秒级时间戳字符串 | `after_agent` 转 int 后作为 STM 消息 `created_at` 和 meta `last_updated_at` |
| `tenant_id` | `configurable_scope` | 默认 `default` | Redis key 维度、Milvus 过滤/写入字段 |
| `user_id` | `configurable_scope` | 字符串；匿名可为 `anonymous` | Redis key 维度、Milvus 字段；可解析为正整数时才写画像 |
| `session_id` | `configurable_scope` 的 `thread_id` | 字符串 | Redis key 维度；数字 session 会作为 MySQL `conversation_id`；LTM 记录动态字段 |
| `user_message` | LangGraph state 最后一条 user | 文本 | MySQL 历史、STM user 消息、压缩 prompt、抽取 prompt |
| `assistant_message` | LangGraph state 最后一条 assistant | 文本 | MySQL 历史、STM assistant 消息、压缩 prompt、抽取 prompt |

##### B. MySQL：事件幂等与可见历史

| 表 | 字段 | 说明 | after_agent 关系 |
|---|---|---|---|
| `processed_events` | `event_type`, `event_id`, `stream_name`, `stream_entry_id`, `payload_hash`, `status`, `attempts`, `lease_owner`, `lease_expires_at`, `last_error`, `created_at`, `updated_at`, `completed_at`, `dead_lettered_at` | Redis Stream 消费端 Inbox；唯一键 `event_type + event_id` | 在进入 `handle_turn_completed` 前认领；确保只有 PROCESS 分支会继续写历史和记忆 |
| `messages` | `id`, `conversation_id`, `sender`, `content`, `message_type`, `turn_event_id`, `created_at` | append-only 对话历史；唯一键 `conversation_id + turn_event_id + sender` | 消费者先 `_persist_turn_history` 写 user/assistant 两行，再调用 `after_agent` |

`messages.conversation_id` 来自数字型 `session_id`；如果 `session_id` 不是数字，源码直接跳过 MySQL 历史落库，但仍可继续走 STM/LTM 记忆写入。

##### C. Redis STM：Session 级 key 与值结构

| Key 逻辑名 | 精确格式 | Redis 类型 | 写入/读取字段 |
|---|---|---|---|
| `messages` | `agent:stm:{tenant_id}:{user_id}:{session_id}:messages` | ZSET | member 是 `compress_message(MessageRecord)` 的二进制 MsgPack/Zstd；score 由 `message_score` 生成毫秒值 |
| `meta` | `agent:stm:{tenant_id}:{user_id}:{session_id}:meta` | String JSON | `SessionMeta(total_turns,last_updated_at,last_compressed_turn)` |
| `summary` | `agent:stm:{tenant_id}:{user_id}:{session_id}:summary` | String JSON | `SessionSummary(content,compressed_at,compressed_round)`；只有压缩 LLM 返回可解析 JSON 时写入 |
| `turns` | `agent:stm:{tenant_id}:{user_id}:{session_id}:turns` | Hash | field=`turn_id`，value=`turn_index`；用于回合级幂等 |
| `turn_lock` | `agent:stm:{tenant_id}:{user_id}:{session_id}:turn_lock` | String lock | `append_turn_once` 的 session 短锁，避免同 session 并发回合互相穿插 |
| `lock` | `agent:stm:{tenant_id}:{user_id}:{session_id}:lock` | String lock | `compress_session_memory` 的压缩锁，避免多消费者同时重写 summary/messages |

`MessageRecord` 的实际字段：

| 字段 | after_agent 写入规则 |
|---|---|
| `message_id` | 有 `turn_id` 时分别为 `msg_u_{turn_id}` / `msg_a_{turn_id}`；旧兼容路径为 `msg_u_{now_ts}` / `msg_a_{now_ts}` |
| `role` | 固定写 `user` 与 `assistant` 两条 |
| `content` | 分别来自 `user_message` / `assistant_message` |
| `created_at` | `event_created_at` 或当前秒级时间 |
| `turn_index` | `meta.total_turns` 自增后的值；重放时不自增 |

`SessionMeta` 的流转：`append_turn_once` 或旧路径先把 `total_turns += 1`、`last_updated_at = now_ts`；压缩成功后 `compress_session_memory` 再把 `last_compressed_turn = total_turns`。

##### D. Milvus LTM：`customer_agent_long_memory` collection

| 字段 | 类型/来源 | 写入时机 | 说明 |
|---|---|---|---|
| `memory_id` | VARCHAR 主键；无 `turn_id` 用 uuid4，有 `turn_id` 用 uuid5 | `save_memory` | 有确定性 ID 时走 upsert，否则 insert |
| `tenant_id` | 字符串入参 | `save_memory` | 检索、去重、软删过滤条件之一 |
| `user_id` | 字符串入参 | `save_memory` | 检索、去重、软删过滤条件之一 |
| `session_id` | dynamic field，来自入参 | `save_memory(session_id=session_id)` | schema 开了 `enable_dynamic_field=True`，用于会话删除时按 session 软删 |
| `memory_type` | `issue_history` / `solution_note` | `memory_extractor.extract` 返回 | 去重时同类型内比较 |
| `content` | 抽取出的语义记忆正文 | `save_memory` | 同时参与 embedding 与 BM25 sparse_vector |
| `embedding` | `embedding_model.embed_query(content)` | `save_memory` | 1024 维 FLOAT_VECTOR，COSINE IVF_FLAT 索引 |
| `sparse_vector` | Milvus BM25 function 由 `content` 生成 | collection 内置函数 | SPARSE_INVERTED_INDEX，支持混合检索 |
| `created_at` / `updated_at` | `SimpleLongTermMemory._now_ts()` | `save_memory` | 注意不是 `event_created_at`，而是 LTM 写入发生时刻 |
| `last_hit_at` | 初始 0，命中刷新时更新 | `update_memory_hit_infos` | 当前主图事件路径通常不传命中列表，所以默认不刷新 |
| `hit_count` | 初始 0，命中刷新时 +1 | `update_memory_hit_infos` | 批量 partial upsert |
| `is_deleted` | 初始 false | `save_memory` / 软删 | 会话删除时按 `session_id` soft delete，定时硬清理再物理删除 |

##### E. MySQL 用户画像：`user_profiles` / `user_facts`

| 表 | 字段 | after_agent 写入条件 | 写入规则 |
|---|---|---|---|
| `user_profiles` | `user_id`, `preferred_brand`, `budget_range`, `preferred_category`, `tags`, `language`, `created_at`, `updated_at` | `user_id` 能解析为正整数，且 extractor 返回非空 `profile` | `UserProfileService.upsert_profile_data` 批量 upsert 主字段，写后删除 Redis 缓存 |
| `user_facts` | `id`, `user_id`, `fact_key`, `fact_value`, `version`, `is_active`, `superseded_by`, `created_at`, `updated_at` | `profile.facts` 中存在合法 `{key,value}` | 相同 key 值不变则跳过；值变化则旧事实置 inactive，新事实 version+1，并回填 superseded_by |
| Redis 画像缓存 | `user:profile:{user_id}` | 读取画像时使用，写入画像后删除 | TTL=`memory.user_profile_cache_ttl`，默认 1800 秒 |

#### 3.2.5 字段流转：一条回答如何扩散成多份物化数据

| 字段/数据 | 第 1 站：事件 | 第 2 站：历史与 Inbox | 第 3 站：STM | 第 4 站：压缩/LTM/画像 |
|---|---|---|---|---|
| `turn_id` | `after_response` 生成，并同时写入 `event_id` | `processed_events.event_id` 防重复消费；`messages.turn_event_id` 防重复历史 | `turns[turn_id] = turn_index`；消息 ID 带 `turn_id` | 参与 uuid5 生成确定性 `memory_id` |
| `event_created_at` | payload 中用字符串传输 | 不直接写 messages 的 `created_at`，MySQL 使用 DB 默认时间 | `now_ts`，写入 MessageRecord.created_at 和 meta.last_updated_at | LTM 使用自身 `_now_ts()`，不是这个时间 |
| `tenant_id` | payload 原样传输 | Inbox 不关心业务含义 | Redis key 前缀维度 | Milvus `tenant_id`，检索/去重/软删过滤条件 |
| `user_id` | payload 原样传输 | MySQL history 不写 user_id，只按 session/conversation 写 | Redis key 前缀维度 | Milvus `user_id`；可解析为正整数时写用户画像 |
| `session_id` | payload 原样传输 | 数字型时作为 `conversation_id` 追加 messages | Redis key 前缀维度 | LTM dynamic field `session_id`，支持删会话软删相关记忆 |
| `user_message` | payload 原样传输 | `messages(sender=user, content=...)` | `MessageRecord(role=user, content=...)` | 压缩 prompt 与抽取 prompt 的输入 |
| `assistant_message` | payload 原样传输 | `messages(sender=assistant, content=...)` | `MessageRecord(role=assistant, content=...)` | 压缩 prompt 与抽取 prompt 的输入 |
| `SessionMeta.total_turns` | 无 | 无 | 首次处理 turn 时 +1；重放不变 | 参与 `should_compress` 与 `compressed_round` |
| `SessionSummary` | 无 | 无 | 压缩成功且 LLM 输出可解析 JSON 时写 `summary` key | 作为 `memory_extractor.extract` 的第三个参数；`None` 也可传入 |
| 语义记忆候选 | 无 | 无 | 无 | extractor 返回 `MemoryExtractorResult`；先 dedup，再 save_memory |
| 画像候选 | 无 | 无 | 无 | extractor 返回 dict；只在数字 user_id 且 profile 非空时 upsert |
| `long_term_memories` 命中列表 | 当前事件路径未传 | 无 | 无 | 若调用方传入，则批量更新 `hit_count/last_hit_at` |

#### 3.2.6 源码级关键分支说明

1. **为什么先写 STM 再压缩/抽取？** 只有本轮 user/assistant 已进入 STM 后，`get_message_count` 和 `total_turns` 才能反映最新状态；压缩也需要基于最新窗口切分旧消息和保留消息。
2. **为什么不是每轮都抽取 LTM？** 源码只在 `should_compress` 为真且 `compress_session_memory` 返回 `True` 后才调用 `memory_extractor.extract`，目的是控制抽取 LLM 成本，并让数轮对话形成更完整的语义片段。
3. **`should_compress` 的精确条件**：`compression.enabled` 为真，且满足 `total_turns - last_compressed_turn >= trigger_rounds` 或 `message_count >= trigger_messages`。默认阈值来自配置：`trigger_rounds=6`、`trigger_messages=20`、`keep_recent_rounds=4`。
4. **压缩成功不等于 summary 一定非空**：`persist_summary_from_messages` 只有在 LLM 返回可解析的 `SessionSummary` JSON 时才写 summary；但压缩管线执行成功后仍会重写最近消息并更新 `last_compressed_turn`，随后 extractor 会拿 `get_summary` 的结果继续工作。
5. **LTM 去重失败默认不写**：`deduplicate_memory` 出错或 embedding 为空时返回 `False`，`after_agent` 会跳过该候选，避免在依赖异常时写入重复/低质量长期记忆。
6. **画像是结构化链路，不写 Milvus**：extractor 返回的 `profile` 走 `profile_writer`，最终进入 MySQL `user_profiles/user_facts`；Milvus 只保存 `issue_history` 和 `solution_note` 两类语义记忆。
7. **异常隔离粒度**：STM 写、压缩抽取、命中统计三段分别捕获异常；`_degrade_once` 按 key 去重，首次通过 `log_degradation` 区分外部依赖 warning 与代码缺陷 error。

---

## 3.9 STM Redis 客户端：必须二进制安全

STM 消息经 `compress_message` 压缩为二进制 MsgPack/Zstd（首字节 `\x00`-`\x03`），
**不是合法 UTF-8**。因此 STM 客户端必须用
`create_stm_redis_client()`（`decode_responses=False`）构造。

> 踩坑记录（v3.34.0 修复）：曾用 `decode_responses=True` 的客户端——
> 写入正常，`zrevrange` 读取时 redis-py 严格 UTF-8 解码抛
> `UnicodeDecodeError`，被降级逻辑吞掉，表现为**短期记忆写得进、读不出，
> 每轮对话拿到的最近消息恒为空**。读路径（`decode_model`/`decode_messages`）
> 原生支持 bytes；画像缓存存 JSON 文本，`json.loads` 同样接受 bytes。

## 4. 短期记忆 STM（Redis）

文件：`stm/redis_short_term_memory.py`、`stm/stm_compressor.py`  
配置：`AppConfig.memory.stm`（`app_config.py`）

### 4.1 默认配置

配置对象：`AppConfig.memory.stm`（嵌套，非扁平）：

| 路径 | 默认 |
|---|---|
| `stm.enabled` | True |
| `stm.time_window_seconds` | 86400 |
| `stm.redis.key_prefix` | `agent:stm` |
| `stm.redis.ttl_seconds` | 86400 |
| `stm.redis.lock_ttl_seconds` | 10 |
| `stm.window.max_messages` | 16 |
| `stm.compression.enabled` | True |
| `stm.compression.trigger_rounds` | 6 |
| `stm.compression.trigger_messages` | 20 |
| `stm.compression.keep_recent_rounds` | 4 |

### 4.2 关键操作

| 方法 | 作用 |
|---|---|
| `append_message` | 写入消息（压缩存储，见 compressor） |
| `append_turn_once` | 有 `turn_id` 的原子回合写入：短锁 + 两条消息/meta/turns 标记同事务；重放不重复追加 |
| `get_recent_messages` | 窗口内最近消息 |
| `get_summary` / **`save_summary`** | 会话摘要读写 |
| `get_meta` / `save_meta` | 轮次元信息 |
| `should_compress` | 是否触发压缩 |
| `compress_session_memory` | 旧消息 → LLM 摘要，保留最近 K 轮 |
| `refresh_ttl` | 滑动过期 |
| `clear_session` | 删除会话全部 STM key（messages/summary/meta/lock/turns/turn_lock） |

### 4.3 压缩存储

`stm_compressor.py`：MsgPack + Zstd 多级压缩，降低 Redis 内存占用。

### 4.4 Key 设计（逻辑）

通常按 `tenant/user/session` 维度区分消息列表、meta、summary、压缩锁，以及
`turns`（已处理 `turn_id → turn_index`）和 `turn_lock`（幂等回合短锁）；具体 key
拼接以 `STMRedisConfig.key_prefix` 与实现为准。

---

## 5. 长期记忆 LTM（Milvus）

文件：`ltm/simple_long_term_memory.py`、`ltm/ltm_collection.py`  
共享检索核：`app/shared/retrieval/milvus_hybrid_core.py`

### 5.1 能力

| 方法 | 作用 |
|---|---|
| `hybrid_search` | 向量 + 关键词类融合检索（RRF 思路） |
| `save_memory` | 写入语义记忆（含可选 `session_id`） |
| `deduplicate_memory` | 近重复抑制 |
| `update_memory_hit_info` | 命中次数/时间 |
| `soft_delete_session_memories` | 按 `session_id` 软删除会话级 LTM |

### 5.2 类型

仅两种语义类型进入 LTM：

- `issue_history`  
- `solution_note`  

用户画像**不再**作为 LTM 类型写入（走 MySQL 画像链路）。

### 5.3 Embedding

容器创建时：

- `EMBEDDING_TYPE == ollama` → `OllamaEmbeddings`  
- 否则 → `HuggingFaceEmbeddings`  

模型名来自 settings（默认 bge-m3 相关配置）。

---


### 5.4 SimpleLongTermMemory 方法过程

文件：`ltm/simple_long_term_memory.py` + `ltm_collection.py`

| 方法 | 过程 |
|---|---|
| `save_memory` | 默认生成 memory_id；若传入事件派生的 `memory_id` 则写 `session_id` 后走 upsert，否则 insert；用于 ACK 前重放的 LTM 防重 |
| `deduplicate_memory` | 同 tenant/user/type 下向量近邻检索；score≥阈值则视为重复不写 |
| `hybrid_search` | filter：`tenant_id`+`user_id`+`is_deleted==false`；dense+sparse hybrid；limit 常为 top_k 的倍数再截断 |
| `update_memory_hit_info` | hit_count+1、last_hit_at=now，upsert |
| `soft_delete_session_memories` | query 匹配 tenant/user/session_id 且未删除记录 → partial upsert `is_deleted=true` |

过滤构造示例逻辑：

```text
tenant_id == "..." and user_id == "..." and is_deleted == false
# 可选 and memory_type == "issue_history"
```

与文档 RAG 的 **collection 名必须分离**（LTM vs `rag_documents`）。


## 6. 记忆抽取器

文件：`orchestration/memory_extractor.py`

```text
extract(user_message, assistant_message, session_summary?)
  → LLM JSON 输出
  → 过滤寒暄/敏感/无价值
  → (List[MemoryExtractorResult], UserProfileData)
```

画像字段通过 `user.domain.profile_payload_support.normalize_profile_data` 规范化。

---

## 7. 画像适配器（跨域端口）

文件：`orchestration/profile_adapter.py`

```text
load_user_profile(user_id, redis) → user_profile_service.get_profile
save_user_profile(user_id, profile, redis) → upsert_profile_data
```

**允许** knowledge → user.application；**禁止** user → knowledge。

---

## 8. 主图侧组装

文件：`chat/.../graph/memory_context.py`

### 8.1 `build_memory_context`

按 P0–P3 拼装带标题的多段文本 + 冲突优先级说明。

### 8.2 `enrich_question`

`原始问题 + 记忆上下文` → 供检索/ReAct 使用的增强问题。

### 8.3 `load_memory_state`

- 若 `state.memory_state` 已有则复用  
- 否则调 middleware.before_agent 并写回 state（请求内缓存）  

---

## 9. 与 API / 会话 ID 的关系

| 来源 | 映射 |
|---|---|
| Form `user_id` | configurable.user_id → 画像 + LTM 用户维 |
| Form `conversation_id` / 生成的 UUID | configurable.thread_id → STM session_id |
| tenant | 默认 `default`（多租户预留） |

**重要：** MySQL `conversations.id` 是整数；LangGraph `thread_id` 是字符串。前端应把 SSE 返回的 `X-Conversation-ID` 原样回传，才能命中同一 STM 会话。若只传 MySQL 数字 id 的字符串形式，需保证创建会话与问答使用同一约定。

---

## 10. 失败降级矩阵

| 故障 | before_agent | after_agent |
|---|---|---|
| Redis 挂 | 无短期上下文 | 写失败，对话仍返回用户 |
| MySQL 画像挂 | 空画像 | 画像更新失败 debug 日志 |
| Milvus 挂 | 无 LTM | 不落长期记忆 |
| LLM 抽取挂 | — | 压缩/抽取跳过 |

原则：**记忆从不可用不应导致主回答 500**（after_response 已吞异常）。

---

## 11. 相关测试

- `tests/knowledge/test_memory_middleware.py`  
- `tests/knowledge/test_redis_short_term_memory_support.py`  
- `tests/knowledge/test_simple_long_term_memory.py`  
- `tests/knowledge/test_memory_extractor.py`  
- `tests/knowledge/test_stm_clear_session.py`  
- `tests/knowledge/test_ltm_soft_delete_session.py`  
- `tests/chat/test_delete_conversation_memory_cleanup.py`  
- `tests/chat/test_lg_memory_runtime.py`、`test_lg_context.py`  

---



---

## 13. Redis 键精确格式（运维对照）

源码：`build_session_keys`（`redis_short_term_memory.py`）

```text
{prefix}:{tenant_id}:{user_id}:{session_id}:messages
{prefix}:{tenant_id}:{user_id}:{session_id}:summary
{prefix}:{tenant_id}:{user_id}:{session_id}:meta
{prefix}:{tenant_id}:{user_id}:{session_id}:lock
```

默认 `prefix = agent:stm`。

| 后缀 | 内容 |
|---|---|
| `messages` | 压缩后的 MessageRecord（MsgPack+Zstd，见 stm_compressor） |
| `summary` | SessionSummary JSON |
| `meta` | SessionMeta JSON（total_turns / last_compressed_turn…） |
| `lock` | 压缩时防并发 |

画像缓存（user 域服务）：

```text
user:profile:{user_id}   # TTL ≈ user_profile_cache_ttl，写成功则 DELETE
```

示例：`tenant=default, user_id=1, thread_id=42`

```text
agent:stm:default:1:42:messages
agent:stm:default:1:42:summary
agent:stm:default:1:42:meta
user:profile:1
```

排查：`docker compose exec redis redis-cli` → `SCAN`/`GET`（生产避免无约束 KEYS）。

---

## 14. MemoryExtractor 内部过程

文件：`orchestration/memory_extractor.py`

```text
extract(user_message, assistant_message, session_summary?)
  1. extract_summary_text(session_summary)  # SessionSummary→纯文本
  2. 组 prompt，调用 self.llm_client.ainvoke
  3. extract_response_text(response)  # 兼容 str / content list
  4. parse_llm_response → parse_first_json_object
  5. build_semantic_memories(parsed):
       - 只保留配置允许的 memory_type（issue_history / solution_note）
       - 过滤寒暄集合（你好/谢谢/…）
       - 过滤过短 content
       - 手机号脱敏 1xx****xxxx；身份证/银行卡/邮箱 pattern 处理
       - 敏感词 pattern 命中则丢弃
  6. profile = normalize_profile_data(parsed.get("profile"))
  7. 返回 (semantic_memories, profile)
```

调用点：仅在 `after_agent` **压缩成功且 ltm_enabled** 时触发（不是每轮都抽）。

---

## 15. STM 压缩触发与步骤（实现级）

```text
should_compress(total_turns, last_compressed_turn, msg_count):
  默认 compression.enabled
  且 (total_turns - last_compressed_turn >= trigger_rounds=6
      或 msg_count >= trigger_messages=20)

compress_session_memory(...):
  1. 加 lock key
  2. 取较旧消息（保留 keep_recent_rounds=4 对应的最近消息）
  3. summary_compressor(old_summary, old_messages) → LLM 文本
  4. 解析/保存 SessionSummary
  5. 裁剪 messages 窗口
  6. 更新 last_compressed_turn
  7. 释放 lock
```

消息编解码：`compress_message` / `decompress_message`（MsgPack，可选 Zstd）。



---

## 面试深挖：记忆系统

### Q1. P0>P1>P2>P3 的业务理由？

| 优先级 | 可信度逻辑 |
|---|---|
| P0 最近对话 | 用户刚说的，最新意图 |
| P1 画像 | 跨会话稳定偏好，但可能过时 |
| P2 摘要 | 压缩有损，可能丢细节 |
| P3 LTM | 历史语义，噪声/过期风险最高 |

冲突时以最近对话为准，避免「旧画像锁死新需求」。

### Q2. 消息压缩的字节格式？（非常容易被问）

`compress_message`：

| packed 长度 | 前缀 flag | 算法 |
|---|---|---|
| ≤2KB | `0x00` | 仅 MsgPack |
| ≤4KB | `0x01` | MsgPack + Zstd level 1 |
| ≤16KB | `0x02` | MsgPack + Zstd level 3 |
| >16KB | `0x03` | MsgPack + Zstd level 9 |

解压：flag∈{1,2,3} 走 ZstdDecompressor，否则纯 MsgPack。**解压端不需要知道 level**。

### Q3. 为何不是每轮都抽 LTM？

只在 **压缩成功且 ltm_enabled** 时 `extract`：  
- 控制 LLM 成本  
- 压缩点=信息沉淀点，语义更完整  
- 减少寒暄轮次污染长期库  

### Q4. 敏感信息怎么处理？

配置 `SENSITIVE_PATTERNS`：密码/验证码/身份证/银行卡/token/手机号。  
Extractor：手机号脱敏 `138****5678` 式；命中敏感则丢弃该条语义记忆。

### Q5. before_agent 部分失败会怎样？

Redis 挂 → 空 STM；画像挂 → {}；Milvus 挂 → []。  
**warn_once** 同类警告只打一次，防日志雪崩。主对话继续。

### Q6. session 维度是什么？

`tenant_id`（默认 default）+ `user_id` + `session_id(=thread_id)`。  
多租户预留 tenant；当前单租户即可。

### Q6.1 删除会话会清哪些记忆？

`DELETE /api/conversations/{id}` 成功后会联动：

| 存储 | 行为 |
|---|---|
| MySQL | 删会话元信息；兼容清理历史 `messages` 行 |
| Redis STM | `clear_session` 删除 messages/summary/meta/lock/turns/turn_lock |
| Milvus LTM | 按 `session_id` 软删除（`is_deleted=true`） |

注意：

- LTM 写入时会带上当前 `session_id`（`after_agent` → `save_memory`）
- 历史无 `session_id` 的 LTM 不会被会话删除命中，避免误删跨会话记忆
- 记忆清理失败不回滚 MySQL 删除（只打日志）

### Q7. LTM 去重阈值 0.88 vs 检索 0.72？

- 检索 0.72：召回可稍松  
- 去重 0.88：更严，避免近重复写入  
面试可说「写路径比读路径更保守」。

## 15.5 关键函数（编排与清理）

### `MemoryMiddleware.before_agent`

```python
async def before_agent(
    self,
    tenant_id: str,
    user_id: str,
    session_id: str,
    user_input: str,
) -> AgentMemoryState
```

三路并发（`asyncio.gather`），任一路失败只降级该路：

| 读取 | 失败降级 |
|---|---|
| `_read_short_term`：`get_summary` ‖ `get_recent_messages` | summary=None，messages=[] |
| `_read_user_profile`：`profile_reader(uid)`（uid 可转 int） | `{}`（匿名返回 None） |
| `_read_long_term`：`milvus_ltm.hybrid_search(...)`（ltm_enabled） | `[]` |

同类错误用 `_degrade_once` 按 key 去重，避免刷屏；首次记录会经
`log_degradation` 分级——外部故障 warning，非预期异常 error + 完整堆栈。

### `MemoryMiddleware.after_agent`（职责摘要）

有 `turn_id` 时，原子 `append_turn_once` 写入本轮 user/assistant、meta 与回合标记；
重放直接跳过重复写入。随后 `should_compress` 则压缩 → 抽取 LTM / 更新画像 → 可选
`update_memory_hit_infos`（批量刷新命中）。
实现细节见上文 §3 / §14–15。

### STM / LTM 清理（删会话用）

```python
# RedisShortTermMemory
async def clear_session(tenant_id, user_id, session_id) -> int  # 删除的 key 数

# SimpleLongTermMemory
async def soft_delete_session_memories(tenant_id, user_id, session_id) -> int
```

由 `ConversationService._clear_conversation_memories` 调用，**不**在 Repository。

### 画像 adapter

```python
async def load_user_profile(user_id: int, redis_client=None) -> UserProfileData
async def save_user_profile(user_id: int, profile: UserProfileData, redis_client=None) -> bool
```

文件：`orchestration/profile_adapter.py` → 转调 `user` 域。

## 16. 下一步

- 画像持久化细节 → [06-用户与画像.md](06-用户与画像.md)  
- LTM 与文档检索共享 → [05-知识检索与文档解析.md](05-知识检索与文档解析.md)

## 学习自测

1. 为什么优先级是 `P0 > P1 > P2 > P3`，而不是把长期记忆放得更高？
2. 为什么系统不直接把全部消息原样塞回 Prompt？
3. 为什么 `after_agent` 里要区分“先写 STM”再“可选压缩 + 抽取 + 更新画像”？
4. 会话删除时，为什么除了 MySQL 元信息，还要去清 Redis 和 Milvus？


---

## 附录 B · 面试重头戏：记忆系统（函数级通关稿）

## B1. 30 秒背诵

```text
读路径 before_agent（可被 load_memory_state 调用）:
  STM.get_summary + get_recent_messages
  profile_reader → user_profile_service
  LTM.hybrid_search(user_input)   # ltm_enabled
  → AgentMemoryState
  → build_memory_context: P0>P1>P2>P3 注入

写路径 after_response → turn_completed(turn_id) → Inbox → after_agent:
  append_turn_once：短锁 + user/assistant + meta + turns 标记同事务
  同一 turn_id 重放直接跳过；再 refresh_ttl
  if should_compress:
      compress_session_memory(LLM summary)
      if compressed and ltm_enabled:
          extractor.extract → dedup → save_memory(session_id=...)
          profile 非空 → profile_writer
  可选 update_memory_hit_info

删会话:
  MySQL delete → clear_session + soft_delete_session_memories
  记忆失败不回滚 MySQL
```

## B2. 优先级 P0–P3（必须能解释冲突）

| 级 | 内容 | 存储 | 冲突时 |
|---|---|---|---|
| P0 | 最近对话 | Redis ZSET | **最高**，刚说的为准 |
| P1 | 用户画像 | MySQL+缓存 | 次之，可能过时 |
| P2 | 会话摘要 | Redis string | 有损压缩 |
| P3 | 长期语义 | Milvus | 最低，噪声风险 |

文案写在 `memory_context._MEMORY_SECTION_TITLES` + `_MEMORY_INSTRUCTIONS`。

## B3. 核心函数签名（背）

### B3.1 Middleware

```python
async def before_agent(self, tenant_id, user_id, session_id, user_input) -> AgentMemoryState
async def after_agent(self, tenant_id, user_id, session_id, user_message, assistant_message,
                      long_term_memories=None, *, turn_id="", event_created_at=None) -> None
```

### B3.2 主图侧

```python
def configurable_scope(config) -> (tenant_id, user_id, session_id)
async def load_memory_state(state, config, user_input) -> AgentMemoryState | None
async def enrich_question(state, config, question) -> str
def build_memory_context(summary, recent, ltm, profile) -> str
def build_enriched_question(question, memory_state) -> str
def format_recent_messages(...) / format_user_profile(...)
```

### B3.3 STM（RedisShortTermMemory）

| 方法 | 作用 |
|---|---|
| `append_message` | ZADD 压缩消息 |
| `get_recent_messages` | 窗口内消息（解码） |
| `get_summary` / **`save_summary`** | 摘要 |
| `get_meta` / `save_meta` | total_turns 等 |
| `get_message_count` | 条数 |
| `should_compress(turns, last_compressed, msg_count)` | 阈值判断 |
| `compress_session_memory(summary_compressor)` | 旧消息→摘要，留 recent rounds |
| `refresh_ttl` | 滑动过期 |
| `clear_session` | 删 messages/summary/meta/lock/turns/turn_lock |

配置嵌套：`AppConfig.memory.stm.redis|window|compression`（不是扁平字段）。

### B3.4 LTM（SimpleLongTermMemory）

| 方法 | 作用 |
|---|---|
| `save_memory(..., session_id="")` | 写向量+稀疏；带 session |
| `deduplicate_memory` | 近重复则不写（阈值~0.88） |
| `hybrid_search` | dense+BM25+RRF，阈值~0.72 |
| `update_memory_hit_info` | 命中计数 partial update |
| `soft_delete_session_memories` | 按 session_id 软删 |
| `hard_purge_soft_deleted` | 定时任务：软删且超保留期 → Milvus 物理删除 |

类型仅：`issue_history` / `solution_note`（画像不进 LTM）。

### B3.5 Extractor

压缩成功后：`MemoryExtractor.extract(user_msg, assistant_msg, summary)`  
→ 语义列表 + 可选 profile dict；敏感模式过滤/手机号脱敏。

### B3.5 硬清理调度

| 配置 `ltm.purge` | 默认 |
|---|---|
| enabled | True |
| interval_seconds | 3600（1 小时） |
| retention_seconds | 7 天 |
| batch_limit | 16384 |

流程：软删（is_deleted=true）→ 超过 retention → 定时任务 `hard_purge_soft_deleted` → Milvus `delete`。  
启动：`AppContainer.start_background_jobs`（lifespan）；关闭时 `stop_background_jobs`。

## B4. Redis Key（运维/面试）

逻辑前缀 `agent:stm`（可配）：

```text
{prefix}:{tenant}:{user}:{session}:messages   # ZSET
{prefix}:{tenant}:{user}:{session}:summary
{prefix}:{tenant}:{user}:{session}:meta
{prefix}:{tenant}:{user}:{session}:lock
```

画像缓存（user 域）：`user:profile:{user_id}` TTL~1800s。

## B5. 压缩触发（数字）

| 配置 | 默认 |
|---|---|
| max_messages 窗口 | 16 |
| trigger_rounds | 6 |
| trigger_messages | 20 |
| keep_recent_rounds | 4 |
| TTL | 86400 |

字节 flag：0x00 仅 msgpack；0x01/02/03 + zstd L1/L3/L9（见面试 Q2 表）。

## B6. 降级矩阵（背）

| 故障 | 读 | 写 |
|---|---|---|
| Redis | 空 STM | 跳过 append（warn_once） |
| 画像服务 | {} | debug 日志 |
| Milvus | [] | 跳过 extract 写 / hit |
| before 整体异常 | memory_state=None，主对话继续 | — |
| after 异常 | — | warning，**用户已收到答案不 500** |

## B7. 删会话 vs 跨会话

- 删会话：STM 全清；LTM **仅**带该 `session_id` 的软删  
- 历史无 session_id 的 LTM：**不删**（防误伤跨会话）  
- 画像：durable，**删会话不删画像**  

## B8. 高频追问

1. **为何压缩点才抽 LTM？** 成本 + 语义完整 + 少寒暄污染。  
2. **去重 0.88 vs 检索 0.72？** 写严读松。  
3. **P0 与 P1 冲突？** 跟最近对话。  
4. **MsgPack 为何还要 Zstd？** 长消息省 Redis 内存。  
5. **lock 干嘛？** 压缩并发互斥。  
6. **tenant 现状？** 预留，默认 default。  
7. **profile 在哪个域？** user 拥有；knowledge 只 adapter。  
8. **after 写失败用户感知？** 答案已出，最多丢记忆。  
9. **session_id 从哪来？** configurable.thread_id。  
10. **clear_session 返回值？** 删除 key 数量（int）。

## B9. 口述演示

「每轮问答前 before_agent 拼四层记忆，P0 优先。答完先发布带 turn_id 的事件，
Inbox 认领后 after_agent 原子写两条消息；重放不重复加轮次。够轮次就压缩，压缩成功
才 extract 进 Milvus 并可能更新画像。删会话时库表和 STM/LTM 一起清，但画像保留。」

---

## 附录 M · 函数手册：记忆系统逐函数明细

> 签名与源码逐字一致（v3.35.1）。📌=每轮对话热路径 🔒=并发语义 ⚠️=历史踩坑。
> 概念叙事见正文 §1–§10；本附录供对照源码逐函数精读。

### M.1 `orchestration/memory_middleware.py`

#### `class MemoryMiddleware.__init__(self, redis_stm, milvus_ltm, memory_extractor, profile_reader=load_user_profile, profile_writer=save_user_profile)`

五依赖全可注入（测试全替身）；读取 `ltm.enabled` 快照；
`_errors_warned: set[str]` 支撑按类去重告警。

#### `def _degrade_once(self, key, operation, exc, **context) -> None`

同类降级只记一次（首个已带堆栈足够定位，不让同一问题每轮刷屏）；
实际分级交给 `log_degradation`（外部故障 warning / 疑似缺陷 exception）。
⚠️ 前身 `_warn_once` 只打固定文案无异常对象——缺陷和抖动在日志里长一样。

#### 📌 `async def before_agent(self, tenant_id, user_id, session_id, user_input) -> AgentMemoryState`

**读扩散入口**（正文 §2.5.3）。三路 `asyncio.gather` 并发：

| 私有方法 | 读什么 | 失败降级 |
|---|---|---|
| `_read_short_term(t,u,s) -> (summary\|None, messages)` | STM 摘要+最近消息（内部再 gather 两条） | `(None, [])` |
| `_read_user_profile(user_id) -> profile \| None` | uid 可转正整数才读；匿名返回 None（不覆盖默认画像） | `{}` |
| `_read_long_term(t,u,input) -> list[MemorySearchResult]` | ltm.enabled 才做 hybrid_search | `[]` |

任一路失败只降级该路；聚合成 `AgentMemoryState` 返回。
并发副作用：三路告警到达顺序不定（测试只断言集合）。

#### 📌 `async def after_agent(self, tenant_id, user_id, session_id, user_message, assistant_message, long_term_memories=None, *, turn_id="", event_created_at=None) -> None`

**写扩散执行体**（由 turn_completed 消费者调用；正文 §2.5.2）。三段各自容错：

```text
① STM 落盘：有 `turn_id` 时 `append_turn_once` 获取会话短锁 → 检查 `turns`
   Hash；首次以 transaction 写两条消息 + meta + turn 标记，重放返回已有 meta
   → refresh_ttl。无 turn_id 才走兼容的 append_messages + save_meta 路径
② 压缩+抽取：复用①的 meta（⚠️ 曾重复 get_meta 一次）→ should_compress?
   → compress_session_memory(summary_compressor 闭包：build_compression_prompt
   → extractor.llm ainvoke) → 压缩成功且 ltm_enabled →
   extractor.extract → 逐条 deduplicate_memory → save_memory(带 session_id)
   → profile 非空 → profile_writer
③ 命中统计：update_memory_hit_infos(全部命中一次批量 upsert
   ⚠️ 曾逐条往返)
```

WHY 压缩点才抽取：控 LLM 成本 + 积累数轮后语义更完整。

#### `def _parse_numeric_user_id(user_id: str) -> int`

纯函数：可转正整数返回之，否则 0（匿名）。

### M.2 `stm/redis_short_term_memory.py`

#### ⚠️ `def create_stm_redis_client(redis_url: str) -> redis.Redis`

**必须 decode_responses=False**：STM 消息是二进制 MsgPack/Zstd（首字节
\x00–\x03），非法 UTF-8。带解码的客户端写入正常，**zrevrange 读取时
UnicodeDecodeError** 被降级吞掉 → "写得进读不出、最近消息恒为空"
（v3.34 修复的静默故障）。读路径 decode_model/decode_messages 原生支持
bytes；画像缓存 JSON 文本 json.loads 同样接受 bytes。

#### 纯函数层

| 函数 | 签名 | 要点 |
|---|---|---|
| `build_session_keys` | `(key_prefix,t,u,s) -> SessionKeys` | 一次拼 messages/summary/meta/lock/turns/turn_lock 六个 key |
| `decode_model` | `(raw: bytes\|str\|None, model_cls) -> Model\|None` | bytes→utf-8→json→pydantic；非 dict 返回 None |
| `message_score` | `(message) -> int` | created_at 毫秒；秒级历史值（<1e12）回退当前毫秒 |
| `extract_summary_from_response` | `(response) -> SessionSummary\|None` | extract_first_json_object 抠 JSON → 模型化 |
| `decode_messages` | `(raw: list[bytes\|str]) -> list[MessageRecord]` | 逐条 decompress，坏条目 debug 跳过；结果 reverse 成时间正序（zrevrange 是倒序） |
| `split_messages_for_compression` | `(messages, keep_recent_rounds) -> (压缩段, 保留段)` | 一轮≈2 条估算：保留 `rounds*2` 条 |
| `build_runtime_settings` | `(*, config, redis_config, window_config, compression_config) -> ShortTermMemoryRuntimeSettings` | 四个配置节收口成一个 frozen 运行时对象；⚠️ 参数名统一 `runtime`，曾用 `settings` 遮蔽全局同名 |
| `should_compress_session` | `(runtime, *, total_turns, last_compressed_turn, message_count) -> bool` | enabled ∧ (轮差≥6 ∨ 条数≥20) |
| `build_compression_context` | `(*, runtime, keys, meta, message_count, old_summary, all_messages) -> CompressionContext\|None` | 未达阈值 None；否则切分消息并快照 old_summary JSON |

#### Redis 往返合并层（⚠️ v3.33 前 4N 次往返）

| 函数 | 行为 |
|---|---|
| `queue_window_pruning(pipe, key, runtime)` | 三条修剪命令入 pipeline：zremrangebyrank(超 16 条)、zremrangebyscore(超 24h)、expire |
| `prune_message_window(*, redis_client, key, runtime)` | 独立修剪入口：pipeline 一次往返 |
| `rewrite_recent_messages(*, redis_client, key, messages, runtime)` | 压缩后重建窗口：delete+批量 zadd+修剪合并**一次**往返（保留 5 条时 21→1） |
| `persist_summary_from_messages(*, tenant_id,…, llm_compress_func, extract_summary_from_response, save_summary)` | 压缩段非空→LLM→解析→save_summary（解析失败静默不写） |
| 🔒 `run_compression_pipeline(*, redis_client, context, lock_ttl_seconds, update_summary, rewrite_messages, save_meta) -> bool` | `SET lock NX EX=10` 分布式锁；拿不到返回 False（并发轮次只压一次）；finally DEL lock；成功后 meta.last_compressed_turn=total_turns |

#### `class RedisShortTermMemory` 方法

| 方法 | 要点 |
|---|---|
| 📌 `append_message / append_messages` | 兼容无事件 ID 的 zadd(+批量)+修剪 pipeline；失败 log_degradation 吞（写降级） |
| 🔒 `append_turn_once(t,u,s,*,turn_id,user_message,assistant_message,created_at)` | session 短锁；已处理 ID 直接返回；否则 transaction 原子写两条消息、meta、`turns` 标记，防 ACK 前重放重复加回合 |
| 📌 `get_recent_messages(t,u,s,limit=None)` | zrevrange 前 limit(默认 16) → decode_messages；失败 `[]` |
| `get_message_count / get_summary / save_summary / get_meta / save_meta` | 单键读写+TTL；get_meta 失败返回**默认 SessionMeta()**（不是 None——调用方免判空） |
| `should_compress(...)` | 转发纯函数 |
| 📌 `compress_session_memory(t,u,s,llm_compress_func) -> bool` | 四路状态 gather 并发读 → build_context（None→False）→ run_compression_pipeline |
| `refresh_ttl(t,u,s)` | messages/summary/meta 三 key expire gather（`turns` 首次写入时设置同 TTL） |
| `clear_session(t,u,s) -> int` | 一次 delete 六 key；会话删除联动调用 |

### M.3 `stm/stm_compressor.py`

| 函数 | 签名 | 要点 |
|---|---|---|
| `compress_message` | `(message) -> bytes` | MsgPack 后按体积分级：≤2KB 仅打包(\x00)；2–4KB zstd L1(\x01)；4–16KB L3(\x02)；>16KB L9(\x03)——小消息不付压缩 CPU，大消息要压缩比 |
| `decompress_message` | `(data: bytes) -> MessageRecord` | 首字节 ≥\x01 走 zstd（解压器不关心级别）；unpackb→模型 |

### M.4 `ltm/ltm_collection.py` — Milvus 客户端封装

`ensure_memory_collection(client, name) -> bool`：不存在则建 schema
（memory_id 主键/租户/用户/session_id 动态字段承载/content 4096/
embedding 1024 维 IVF_FLAT-COSINE/**服务端 BM25 Function**
content→sparse_vector + SPARSE_INVERTED_INDEX）；返回是否新建。
构造期一次性调用，不在请求路径。

数据面（全部经 `run_blocking` 线程池，⚠️ v3.33 前直接阻塞事件循环）：
`insert_records / upsert_records / search_records / query_records
（None→[] 归一）/ delete_by_filter` 与
`build_primary_key_in_filter(field, keys)`（⚠️ 按主键删而非业务表达式：
部分版本对动态字段参与 delete 不稳）。

### M.5 `ltm/simple_long_term_memory.py`

分层约定：模块级只放**纯函数**（无 IO 可单测）；带 IO 编排全是类方法
（⚠️ 曾把 logger/写入函数当参数层层下传：11 参签名+6 处 type ignore）。

#### 纯函数

| 函数 | 要点 |
|---|---|
| `entity_to_memory(entity) -> LongTermMemory` | 缺省骨架 + 仅覆盖非 None 已知键 |
| `build_active_memory_filter(t,u,memory_type=None)` | `tenant∧user∧is_deleted==false[∧type]` |
| `build_session_memory_filter(t,u,s)` | 会话级软删过滤（依赖动态字段 session_id；无该字段的历史记录不匹配=防误删） |
| `build_expired_soft_deleted_filter(cutoff_ts)` | `is_deleted∧updated_at<cutoff`（软删时刷 updated_at 故可近似软删时间） |
| `build_memory_record / build_partial_update_record / build_new_memory_insert_record` | 全量写入/部分 upsert/新记忆(分配 uuid) 三种 payload |
| `build_hit_update_plan(memory, update_config: LTMUpdateOnHitConfig, now_ts)` | ⚠️ 属性访问 config——曾按 dict 下标 TypeError 被吞致 LTM 静默失效；按开关算 hit_count/last_hit_at |
| `resolve_active_search_request(search_config: LTMSearchConfig, t,u,top_k,score_threshold)` | 显式参优先于配置默认；返回 (filter, top_k, threshold) |
| `search_results_from_hits / has_dedup_match / preview_text / extract_ids` | 命中转换/去重判定(max distance≥阈值)/日志截断/主键提取 |

#### `class SimpleLongTermMemory` 方法

构造：三个配置显式标注类型（`LTMSearchConfig` 等）——**误写回下标访问
会在类型检查期爆**；`ensure_collection_ready_or_raise` + 默认
retrieval_core（可注入）。

| 方法 | 要点 |
|---|---|
| `_get_embedding(text)` | run_blocking(embed_query)；失败 None |
| `save_memory(t,u,type,content,*,session_id="", memory_id=None) -> str\|None` | embed→record；未传 ID 用 insert，传入由 `turn_id` 派生的确定性 ID 时用 upsert，session_id 供会话级清理 |
| `deduplicate_memory(...) -> bool` | True=该存（无足够相似）；检索 top3 距离≥0.88 则 False；异常 False（宁可少存不重复） |
| `update_memory_hit_info(memory)` | 单条→批量的便捷转发 |
| 📌 `update_memory_hit_infos(memories) -> bool` | 开关关/空列表直接 True；一次 upsert 全部命中计划（⚠️ 曾逐条 N 次往返）；同时回写内存对象字段 |
| 📌 `hybrid_search(t,u,query,top_k=None,score_threshold=None)` | resolve 参数→retrieval_core.search_hybrid(search_limit=2×top_k)→转换；异常 `[]`（记忆是增强项不拖垮主链路） |
| `soft_delete_session_memories(t,u,s) -> int` | query 主键(≤16384)→批量 upsert is_deleted=true；空 session 0 |
| `hard_purge_soft_deleted(*, retention_seconds=None, batch_limit=None) -> int` | 参数缺省取 purge 配置；过期软删→按主键物理删除；供调度器循环调用 |

### M.6 `ltm/purge_scheduler.py`

- `_wait_for_stop_or_timeout(stop_event, timeout) -> bool`：可中断睡眠
  （True=收到停机）
- `run_ltm_hard_purge_loop(*, get_ltm, purge_config, stop_event, wait_fn=None)`：
  循环 {等 interval(默认 1h)→get_ltm()（懒取，容器可能未热）→
  hard_purge}；每轮异常吞掉继续（清理循环不许被单轮失败杀死）；
  wait_fn 可注入加速测试。由 `container._start_ltm_purge` 启动。

### M.7 `orchestration/memory_extractor.py`

| 函数/方法 | 要点 |
|---|---|
| `extract_summary_text(summary)` | SessionSummary/str/None → 纯文本（避免把模型对象 repr 进 prompt） |
| `extract_response_text(response)` | 兼容 str / AIMessage.content 列表 |
| `parse_llm_response(response) -> dict` | parse_first_json_object；失败空 dict |
| `build_semantic_memories(parsed, *, sensitive_patterns) -> list[MemoryExtractorResult]` | 校验类型白名单（issue_history/solution_note）+ **敏感正则过滤**（密码/验证码/证件/卡号/token/手机号命中即丢弃该条） |
| `MemoryExtractor.extract(user_message, assistant_message, session_summary) -> (memories, profile)` | 拼抽取 prompt→LLM→解析→语义记忆+画像 dict；异常 `([], {})` 降级 |

### M.8 `orchestration/profile_adapter.py` — 跨域端口

`load_user_profile(user_id, redis_client=None)` /
`save_user_profile(user_id, profile, redis_client=None)`：
把 user 域 `UserProfileService` 适配成 knowledge 域的
ProfileReader/ProfileWriter 协议——knowledge 不 import user 的实现细节，
只依赖这两个函数签名（依赖倒置的落地点）。

### M.9 `domain/prompt_builder.py`

| 函数 | 要点 |
|---|---|
| `build_memory_injection_prompt(long_term_memories)` | P3 注入文本；空/None→"" |
| `build_summary_injection_prompt(session_summary)` | P2 注入文本 |
| `build_compression_prompt(old_summary, old_messages, compressed_round)` | 压缩 LLM 的完整指令（旧摘要+待压缩消息+轮次） |

### M.10 函数 → 测试对照

| 范围 | 测试 |
|---|---|
| middleware 读扩散并发/降级分级/批量命中 | tests/knowledge/test_memory_middleware.py |
| STM pipeline 合并/二进制回归/压缩并发读 | tests/knowledge/test_stm_pipelining.py |
| STM 纯函数/支持函数 | test_redis_short_term_memory_support.py、test_stm_store_utils.py、test_stm_clear_session.py |
| 压缩编码 | test_memory_config* 与 stm 相关用例 |
| LTM 类与纯函数 | test_simple_long_term_memory.py、test_ltm_operation_utils.py（含**下标访问回归**）、test_ltm_collection.py、test_ltm_store_utils.py |
| 软删/硬清/调度 | test_ltm_soft_delete_session.py、test_ltm_hard_purge.py |
| 抽取器 | test_memory_extractor.py |
| 注入 prompt | test_prompt_builder.py、test_lg_memory_prompt.py |
