"""后端根包。

职责：
- 作为 `llm_backend/` 目录的包级标记
- 提供最小化的根包说明，帮助阅读者区分“包入口”和“应用入口”

边界：
- 不把根包本身当作稳定导入面
- 具体能力继续通过 `app/`、`main.py`、`run.py` 等明确入口访问
"""

# 根包不直接暴露稳定 API，避免调用方绕过各子模块已经明确的边界。
__all__: list[str] = []
