"""后端应用主包。

职责：
- 聚合后端运行时的主要子模块
- 为阅读者提供“从入口到业务”的高层导航

边界：
- `api/` 负责 HTTP 协议转换
- `core/` 负责基础设施
- `services/` 负责复用型业务流程
- `lg_agent/` 负责 Agent 主流程编排
- `memory/` 负责记忆读写与中间件
- `models/` 负责持久化结构
- `security/` 负责 Prompt 注入防御相关的轻量工具
"""
